README

This is the submission for the DBMS project for COSC 471 for Ishaq Ahmed, Justin Haynes, Ryan Hastie, and Faisal Abusara.

This folder contains the three output files, file1 with the output from the command line, file2 with the output file generated from running input project-test-data.txt file2.output;, and file3 with the commands that do not work highlighted in red which is only the aggregates that is meant for the graduate students to do.

Aside from the aggregates, every command is working correctly to the best of our knowledge.

The output files need to be opened by using open with because the files all end with .output

The output in file1 and file2 differ as the databases, tables, records, etc generated from the command line run was not cleared before running the input command with the test file. The data was not cleared as it was not specified in the turn in instructions that the data should be cleared.

The folder with our project files still contains all of the data from the two runs so that those folders and files can still be accessed.

To run the code import the project files into an IDE and run the Main.java program where the program will then prompt for commands. 

